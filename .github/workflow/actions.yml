name: This is a github action
on: [push]
jobs: 
  Echo: 
    runs-on: ubuntu-latest
    steps:
       - name: checkout repo
         uses: actions/checkout
       - name: cat temp.txt
         run: cat temp.txt
         
